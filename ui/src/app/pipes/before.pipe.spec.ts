/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BeforePipe } from './before.pipe';

describe('Pipe: Before', () => {
  it('create an instance', () => {
    let pipe = new BeforePipe();
    expect(pipe).toBeTruthy();
  });
});
