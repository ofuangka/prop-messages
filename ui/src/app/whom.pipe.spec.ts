/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WhomPipe } from './whom.pipe';

describe('Pipe: Whom', () => {
  it('create an instance', () => {
    let pipe = new WhomPipe();
    expect(pipe).toBeTruthy();
  });
});
